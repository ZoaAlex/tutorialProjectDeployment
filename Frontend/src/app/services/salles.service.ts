/**
 * SallesService — alias de SalleService pour la compatibilité avec materiel.component.ts
 * Délègue tous les appels à SalleService (service unique).
 */
export { SalleService as SallesService } from './salle.service';
